{
  "name": "codex-acceptance-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A dependency-free local catalog used as a WebMCP implementation acceptance fixture.",
  "engines": {
    "bun": ">=1.3"
  },
  "scripts": {
    "start": "bun server.mjs",
    "dev": "bun --watch server.mjs",
    "check": "bun scripts/check.mjs",
    "test": "bun scripts/smoke.mjs"
  }
}
